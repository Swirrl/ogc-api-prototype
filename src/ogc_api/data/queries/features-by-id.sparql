PREFIX geo: <http://www.opengis.net/ont/geosparql#>

CONSTRUCT {
  ?feature_uris ?p ?o .
  ?geo ?gp ?go .
}
WHERE {
  VALUES ?feature_uris { UNDEF } .

  ?feature_uris geo:hasGeometry ?geo ;
                ?p ?o .
  ?geo ?gp ?go .
}
